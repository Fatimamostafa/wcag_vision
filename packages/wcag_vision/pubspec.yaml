name: wcag_vision
description: >-
  Offline, algorithmic WCAG accessibility engine: colour contrast ratios,
  CVD simulation, and k-means colour extraction. Pure-Dart core.
version: 0.0.1
repository: https://github.com/Fatimamostafa/wcag_vision

# Joins the Dart pub workspace at the repo root (see /pubspec.yaml). Resolution
# is shared across the monorepo; do not add a per-package pubspec.lock.
resolution: workspace

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  # Pure-Dart annotations (@immutable) — used instead of
  # flutter/foundation.dart in modules that avoid Flutter imports.
  meta: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  # very_good_analysis is the mandated base lint set (CLAUDE.md §8), stricter
  # than flutter_lints. Zero issues tolerated under `dart analyze --fatal-infos`.
  very_good_analysis: ^9.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # To add assets to your package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # To add custom fonts to your package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package
