name: wcag_vision
description: >-
  Check color contrast, simulate color blindness, and extract dominant
  colors from images — accessibility tools for Flutter, no expertise
  required.
version: 0.1.0
repository: https://github.com/Fatimamostafa/wcag_vision

# Joins the Dart pub workspace at the repo root (see /pubspec.yaml). Resolution
# is shared across the monorepo; do not add a per-package pubspec.lock.
resolution: workspace

environment:
  sdk: ^3.12.2

dependencies:
  # Pure-Dart annotations (@immutable) — this package has no Flutter
  # dependency, so it never uses flutter/foundation.dart.
  meta: ^1.16.0

dev_dependencies:
  test: ^1.25.0
  # very_good_analysis is the mandated base lint set (CLAUDE.md §8), stricter
  # than flutter_lints. Zero issues tolerated under `dart analyze --fatal-infos`.
  very_good_analysis: ^9.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
